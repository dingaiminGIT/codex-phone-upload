interface:
  display_name: "Phone Upload"
  short_description: "Scan a QR code and attach phone images to the Codex composer"
  default_prompt: "Use $phone-upload in fast same-Wi-Fi mode to generate a QR code and paste selected phone images into the current Codex composer without sending or analyzing them."
